spring:
  application:
    name: hackathon_tusur
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 1000MB
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${ISSUER_URI_OAUTH2:http://localhost:8082/realms/hits-project}
  mail:
    host: smtp.gmail.com
    port: 587
    username: zdraviymalish@gmail.com
    password: nfjz gwye cyxb yplw
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

authServer:
  authorizationUrl: http://localhost:8082/realms/hits-project/protocol/openid-connect/auth
  tokenUrl: http://localhost:8082/realms/hits-project/protocol/openid-connect/token
  refreshUrl: http://localhost:8082/realms/hits-project/protocol/openid-connect/token

amazonProperties:
  endpointUrl: https://storage.yandexcloud.net
  bucketName: hackathon-perm-cup-bucket

yandexApi:
  speechUri: https://transcribe.api.cloud.yandex.net
  operationUri: https://operation.api.cloud.yandex.net
  token: AQVNx1tbPR0KCm51us0xl5MpmqKEVf62k2lBCfpg
  speechEndpoint: /speech/stt/v2/longRunningRecognize
  statusEndpoint: /operations/{id}